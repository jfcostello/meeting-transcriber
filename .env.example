# Required by WhisperX at runtime. The token needs read access and accepted
# terms for pyannote/speaker-diarization-community-1.
HF_TOKEN=

# Intentionally not selected yet. Benchmark the current shortlist first:
# - distil-whisper/distil-large-v3.5-ct2 (English, fastest candidate)
# - large-v3-turbo (multilingual, stronger long-form candidate)
# - large-v3 (accuracy baseline, slowest)
WHISPER_MODEL=
